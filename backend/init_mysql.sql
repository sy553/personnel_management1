-- 创建数据库
CREATE DATABASE IF NOT EXISTS personnel_management DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE personnel_management;

-- 创建用户并授权（如果需要）
-- GRANT ALL PRIVILEGES ON personnel_management.* TO 'your_user'@'localhost' IDENTIFIED BY 'your_password';
